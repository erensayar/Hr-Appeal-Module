server:
  port: 8080
spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:mem:hr-web-app
    username: sa

file-upload:
  storage-path: /Users/erensayar/Downloads/storage

security:
  jwt:
    expire-time: 720000 # 120 minutes -> 120 * 60 * 1000